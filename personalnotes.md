scrollable list for past menus
randomizer works but doesnt show
no way to add snacks to meal/snacks/staples?
grocery list has a way to add snacks but doesnt seem to go to snacks(and no staples?)
maybe refactor grocery list to show a dropdown/expandable stocks/staples on top of grocery list like a banner with clickable + addable options to add quickadd to grocery list(dropdown/expandable would be )


so a few issues/bugs:
1. scrollable list for past menus - works for list view, but calendar view it doesnt. also calendar view gets a lot wider and squishes insights

2. randomizer at the top works but after you press takeout/home theres no output, and if you click it again you can see the output in the badge(when clicked) for example : I click quick pick ->takeout: i see nothing, i click quick pick ->
Takeout: Mcdonalds is now showing underneath the buttons

3. we can add snacks and staples but only after we generate grocery list can we change this? We have add meal button + categories? Could we utilize that and a dropdown with snacks and staples categories? 

Then the grocery list has an expandable 'snacks' section at the top + 'staples' section that we can expand + quick add to grocery list? that way we generate menu -> generate grocery list -> add snacks/staples and we would have a fully sourced grocery list?

4. all meals is long/vertical, it has pagination already can we add an adjustable option to pagination to show only 5/10/20 meals( i think at the moment its at 20)

5. when I reduce page size the columns(our menu+grocery list) side by side, and the (past menus + insights) side by side are now squished on top of each other, was hoping we could resize it and theyd stay side by side

6. we may need to check search meals functionality since it works with category (we'd be adding snacks+staples here) but we use category to filter (set to 'all' at the moment - no categories exist - I havent added any) just wanted to note because we are adding 'snacks' and 'staples' possibly?